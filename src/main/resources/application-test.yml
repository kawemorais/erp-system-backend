spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: "jdbc:h2:mem:db"
    username: "sa"
    password: "sa"
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect:
        format_sql: true
  flyway:
    enabled: false